{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/node"
    }
  ],
  "build": {
    "env": {
      "PYTHON_VERSION": "3.9"
    }
  },
  "hooks": {
    "build": "curl https://pyenv.run | bash && ~/.pyenv/bin/pyenv install 3.9.6 && ~/.pyenv/bin/pyenv global 3.9.6 && npm install"
  }
}
